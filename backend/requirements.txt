# NOT what Vercel installs from — Vercel's Python build reads backend/uv.lock
# (via pyproject.toml's [project.dependencies]). This file is kept only as a
# plain-pip fallback for environments without `uv`; a dependency added here
# without also running `uv lock` will work locally and on CI but silently be
# ABSENT in production (ModuleNotFoundError at cold start) — see ADR-040 in
# docs/DECISIONS.md. Keep in sync with [project.dependencies] in pyproject.toml.
# Versions pinned to what was verified locally (Python 3.14) — see ADR-009 in docs/DECISIONS.md.
fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy==2.0.53
alembic==1.20.0
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
asyncpg==0.31.0
greenlet==3.5.6
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.14.0
structlog==26.1.0
python-multipart==0.0.32
reportlab==5.0.1
openpyxl==3.1.5
